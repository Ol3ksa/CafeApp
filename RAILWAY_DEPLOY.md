# 🚂 Деплой на Railway.app - Покрокова інструкція

Railway - ідеальний вибір для короткострокового використання (5 днів). Безкоштовний план дає 500 годин на місяць, що більш ніж достатньо для 5 днів роботи (120 годин).

## ⚡ Швидкий старт (5 хвилин)

### Крок 1: Реєстрація на Railway

1. Перейдіть на https://railway.app
2. Натисніть "Start a New Project" або "Login"
3. Зареєструйтесь через GitHub (рекомендовано) або Email

### Крок 2: Створення проекту

1. Після входу натисніть **"New Project"**
2. Оберіть **"Deploy from GitHub repo"** (якщо код на GitHub) або **"Empty Project"**
3. Якщо оберете "Empty Project", потім натисніть **"Add Service"** → **"GitHub Repo"**

### Крок 3: Підготовка файлів

Переконайтеся, що у вас є такі файли в проекті:

```
CafeApp/
├── server.py          ✅ (головний файл сервера)
├── Cafe22.html        ✅ (інтерфейс)
├── orders.json        ✅ (файл з даними)
├── requirements.txt   ✅ (залежності Python)
└── railway.json       ✅ (опціонально, налаштування Railway)
```

### Крок 4: Налаштування змінних оточення

В Railway проекті:

1. Виберіть ваш сервіс
2. Перейдіть на вкладку **"Variables"**
3. Додайте такі змінні:

| Змінна | Значення | Опис |
|--------|----------|------|
| `CAFE_ADMIN_PASSWORD` | `ваш_пароль_адміна` | Пароль для ролі "працівник" |
| `CAFE_SCREEN_PASSWORD` | `ваш_пароль_екрана` | Пароль для ролі "екран" |
| `CAFE_SECRET_KEY` | `випадковий_рядок_32_символи` | Секретний ключ для токенів |

**Як згенерувати CAFE_SECRET_KEY:**
- Онлайн: https://randomkeygen.com/ (виберіть "CodeIgniter Encryption Keys")
- Або в Python: `python -c "import secrets; print(secrets.token_urlsafe(32))"`

**Приклад значень:**
```
CAFE_ADMIN_PASSWORD = cafe2024
CAFE_SCREEN_PASSWORD = screen2024
CAFE_SECRET_KEY = Xk8vQ2mN9pL4rT7wY5zA1bC3dE6fG8hJ0k
```

### Крок 5: Деплой

1. Railway автоматично визначить Python і запустить сервер
2. Перевірте вкладку **"Deployments"** - там буде статус збірки
3. Після успішного деплою натисніть на **"Settings"** → **"Generate Domain"**
4. Скопіюйте отриманий URL (наприклад: `cafe-app-production.up.railway.app`)

### Крок 6: Перевірка роботи

1. Відкрийте URL у браузері
2. Перевірте, що відображається форма входу
3. Спробуйте увійти з паролем адміна

## 🔧 Налаштування (опціонально)

### Кастомний домен

Якщо потрібен свій домен:

1. Settings → Domains → Add Custom Domain
2. Додайте ваш домен
3. Налаштуйте DNS записи (Railway покаже інструкції)

### Моніторинг використання

1. Перейдіть на вкладку **"Metrics"**
2. Там ви побачите використання ресурсів
3. Безкоштовний план: $5 кредитів на місяць

## 📊 Вартість

**Для 5 днів роботи:**
- 5 днів × 24 години = **120 годин**
- Безкоштовний план: **500 годин/місяць**
- ✅ **Цілком достатньо безкоштовно!**

Якщо проект простий (мало трафіку), використання буде мінімальним.

## 🛑 Зупинка проекту (після 5 днів)

1. Відкрийте ваш проект в Railway
2. Settings → Danger Zone → Delete Service
3. Підтвердіть видалення

Або просто перестаньте використовувати - Railway автоматично зупинить проект через 5 хвилин бездіяльності (на безкоштовному плані).

## ⚠️ Важливо

1. **Не забудьте змінити паролі!** Не використовуйте дефолтні `admin123`
2. **Збережіть URL проекту** - він буде потрібен для доступу
3. **Регулярно робіть backup** файлу `orders.json` (можна експортувати через інтерфейс)
4. **Видаліть проект** після використання, щоб не витрачати кредити

## 🐛 Вирішення проблем

### Помилка при деплої

1. Перевірте логи в Railway (Deployments → View Logs)
2. Переконайтеся, що `requirements.txt` містить всі залежності
3. Перевірте, що `server.py` має правильний синтаксис

### Сервіс не запускається

1. Перевірте змінні оточення (Variables)
2. Перевірте логи - там буде помилка
3. Переконайтеся, що порт налаштований правильно (Railway використовує змінну PORT)

### Помилка підключення WebSocket

1. Railway автоматично налаштовує WebSocket
2. Переконайтеся, що використовуєте HTTPS (Railway надає автоматично)
3. Перевірте логи сервера

## 📝 Приклад конфігурації

**requirements.txt:**
```
Flask==3.0.0
flask-socketio==5.3.6
python-socketio==5.10.0
itsdangerous==2.1.2
```

**railway.json (опціонально):**
```json
{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python server.py",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
```

## ✅ Чеклист перед деплоєм

- [ ] Всі файли завантажені (server.py, Cafe22.html, orders.json, requirements.txt)
- [ ] requirements.txt містить всі залежності
- [ ] Змінні оточення встановлені (CAFE_ADMIN_PASSWORD, CAFE_SCREEN_PASSWORD, CAFE_SECRET_KEY)
- [ ] Паролі змінені з дефолтних значень
- [ ] server.py використовує змінну PORT (вже налаштовано)
- [ ] Тестовий запуск локально пройшов успішно

## 🎉 Готово!

Після виконання всіх кроків ваша програма буде доступна за URL від Railway. Ви зможете працювати з нею з будь-якого місця, навіть якщо ваш комп'ютер вимкнений!

**Підтримка Railway:** https://railway.app/docs

