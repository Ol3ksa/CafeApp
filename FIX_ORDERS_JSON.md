# 🔧 Виправлення проблеми з orders.json

## Проблема

Після оновлення файлів на GitHub, файл `orders.json` перезаписується старим файлом, і нові замовлення пропадають.

## Рішення

### Крок 1: Додано orders.json в .gitignore ✅

Файл `orders.json` тепер додано в `.gitignore`, тому він не буде синхронізуватися з GitHub.

### Крок 2: Видалити orders.json з Git репозиторію

Якщо файл `orders.json` вже є в вашому GitHub репозиторії, потрібно його видалити:

**Через командний рядок (Git Bash або Terminal):**

```bash
# 1. Переконайтеся, що ви в папці проекту
cd C:\Users\User\Desktop\CafeApp

# 2. Видаліть файл з Git (але залиште локально)
git rm --cached orders.json

# 3. Зробіть commit
git commit -m "Remove orders.json from repository - will be auto-created on server"

# 4. Запуште зміни на GitHub
git push
```

**Через GitHub веб-інтерфейс:**

1. Відкрийте ваш репозиторій на GitHub
2. Знайдіть файл `orders.json`
3. Натисніть на нього
4. Натисніть кнопку "Delete" (корзина)
5. Зробіть commit з повідомленням "Remove orders.json from repository"

### Крок 3: Перевірка на Railway

Після наступного деплою на Railway:

- Файл `orders.json` буде автоматично створений на сервері при першому замовленні
- Всі замовлення будуть зберігатися на сервері Railway
- При оновленні коду з GitHub, файл `orders.json` НЕ буде перезаписаний

### Крок 4: Backup (рекомендовано)

Для безпеки, періодично робіть backup замовлень:

1. Відкрийте ваш Railway проект
2. Перейдіть в Settings → Volumes (якщо використовуєте)
3. Або експортуйте дані через інтерфейс програми

## ✅ Результат

Після виконання цих кроків:

- ✅ `orders.json` не буде синхронізуватися з GitHub
- ✅ Дані будуть зберігатися тільки на Railway сервері
- ✅ Оновлення коду не вплине на існуючі замовлення
- ✅ Нові замовлення не будуть пропадати

## ⚠️ Важливо

**НЕ видаляйте** локальний файл `orders.json` з вашого комп'ютера, якщо він містить важливі дані!
Видаліть його тільки з Git репозиторію командою `git rm --cached orders.json`.

Якщо потрібно зберегти поточні дані:

1. Зробіть копію файлу `orders.json` (наприклад, `orders.json.backup`)
2. Потім видаліть його з Git репозиторію
